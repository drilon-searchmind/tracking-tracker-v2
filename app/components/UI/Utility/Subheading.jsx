import React from 'react'

const Subheading = ({ headingText }) => {
    return (
        <h2 className="font-semibold text-base text-[var(--color-primary-searchmind-lighter)] mb-2 capitalize">{headingText}</h2>
    )
}

export default Subheading